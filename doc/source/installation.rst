============
Installation
============

At the command line::

    $ pip install python-cognitiveclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-cognitiveclient
    $ pip install python-cognitiveclient
